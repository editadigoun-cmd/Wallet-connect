export function show(id){document.querySelectorAll(".screen").forEach(s=>s.classList.toggle("active",s.id===id));document.querySelectorAll("[data-screen]").forEach(b=>b.classList.toggle("active",b.dataset.screen===id&&b.closest(".bottom")));const nav=document.querySelector(".bottom"),header=document.getElementById("mainHeader");nav.style.display=id==="auth"?"none":"grid";header.style.display=id==="auth"?"none":"flex";document.body.style.paddingBottom=id==="auth"?"0":"92px";window.scrollTo({top:0,behavior:"smooth"})}
export function initNavigation(){document.querySelectorAll("[data-screen]").forEach(b=>b.addEventListener("click",()=>show(b.dataset.screen)));}
